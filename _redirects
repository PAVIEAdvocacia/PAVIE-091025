# Exceções (devem vir primeiro)
 /api/*            /api/:splat           200
 /blog/admin/*     /blog/admin/:splat    200

# Fallback SPA (opcional). Mantenha por último se precisar.
# /*                 /index.html          200

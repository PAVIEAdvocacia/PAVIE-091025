---
import '../styles/blog.css';

type Props = {
  data: {
    title: string;
    subtitle?: string;
    category?: string;
    pubDate?: Date | string;
    updatedDate?: Date | string;
    readingTime?: number;
  };
  Content: any;
};

const { data, Content } = Astro.props as Props;
---

<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <title>{data.title} | PAVIE | Advocacia</title>
    <meta name="description" content={data.subtitle ?? ''} />
  </head>
  <body>
    <main>
      <article class="blog-post">
        <header class="blog-post-header">
          <p class="blog-post-kicker">
            {data.category ?? 'Conteúdo Jurídico'}
          </p>
          <h1 class="blog-post-title">
            {data.title}
          </h1>
          {data.subtitle && (
            <p class="blog-post-subtitle">
              {data.subtitle}
            </p>
          )}
          <p style="margin-top: 0.75rem; font-size: 0.8rem; color: #94a3b8;">
            {data.pubDate && (
              <>
                Publicado em {new Date(data.pubDate).toLocaleDateString('pt-BR')}
              </>
            )}
            {data.updatedDate && (
              <> • Atualizado em {new Date(data.updatedDate).toLocaleDateString('pt-BR')}</>
            )}
            {data.readingTime && (
              <> • {data.readingTime} min de leitura</>
            )}
          </p>
        </header>

        <section class="blog-post-content">
          <Content />
        </section>

        <footer class="blog-post-footer">
          <p>
            Este texto tem caráter exclusivamente informativo e não substitui consulta
            jurídica individualizada. Em caso concreto, recomenda-se análise técnica
            específica.
          </p>
        </footer>
      </article>
    </main>
  </body>
</html>

---
import BrandLogo from '../components/brand/BrandLogo.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import { buildFaqSchema, LEGAL_SERVICE_BASE_SCHEMA, toAbsoluteUrl } from '../lib/seo';
import { MAIN_SITE_URL } from '../consts';
import { getAreaEntries, getAuthorEntries, getBlogEntries } from '../lib/blog-content';
import { buildCanonicalAreaDirectory } from '../lib/canonical-directory';
import { CANONICAL_CATEGORY_DEFINITIONS } from '../lib/canonical-content';
import { isPublicPost, normalizePosts, sortPostsByDate } from '../lib/posts';
import '../styles/site-home.css';

const title = 'PAVIE | Advocacia | Clareza, tÃ©cnica e respeito';
const description =
	'Advocacia estratÃ©gica e acolhedora para questÃµes de patrimÃ´nio, famÃ­lia, curatelas, sucessÃµes, imÃ³veis, contratos, crÃ©ditos e direitos do consumidor.';

const processSteps = [
	{
		title: 'AnÃ¡lise inicial',
		text: 'Ouvimos vocÃª para entender a situaÃ§Ã£o real. Avaliamos a viabilidade com franqueza antes de prometer qualquer resultado.',
	},
	{
		title: 'Leitura documental',
		text: 'Analisamos cuidadosamente os seus documentos, certidÃµes e histÃ³rico para garantir que nÃ£o haja surpresas no caminho.',
	},
	{
		title: 'EstratÃ©gia e opÃ§Ãµes',
		text: 'Apresentamos as rotas disponÃ­veis, os riscos envolvidos e os custos de forma totalmente clara e sem jargÃµes desnecessÃ¡rios.',
	},
	{
		title: 'ExecuÃ§Ã£o segura',
		text: 'Atuamos na defesa dos seus direitos, mantendo vocÃª atualizado e seguro sobre cada avanÃ§o do seu caso.',
	},
];

const faq = [
	{
		q: 'O primeiro contato jÃ¡ significa contrataÃ§Ã£o?',
		a: 'NÃ£o. O contato inicial serve para triagem, compreensÃ£o do contexto e verificaÃ§Ã£o de aderÃªncia. A contrataÃ§Ã£o depende de escopo definido, proposta formal e aceite expresso.',
	},
	{
		q: 'Quais documentos devo separar antes de enviar a situaÃ§Ã£o?',
		a: 'Comece pelos documentos pessoais, contratos, certidÃµes, mensagens, notificaÃ§Ãµes, registros, prazos e qualquer prova que ajude a reconstruir a cronologia do caso.',
	},
	{
		q: 'O atendimento pode ocorrer fora do Rio de Janeiro?',
		a: 'Sim. A anÃ¡lise inicial pode ocorrer online, com avaliaÃ§Ã£o do que pode ser conduzido a distÃ¢ncia e do que depende de providÃªncia local ou complementar.',
	},
	{
		q: 'Como os honorÃ¡rios sÃ£o definidos?',
		a: 'Os honorÃ¡rios dependem do tipo de demanda, complexidade, urgÃªncia, documentos disponÃ­veis e extensÃ£o do acompanhamento. Quando houver aderÃªncia, a proposta Ã© apresentada de forma formal e clara.',
	},
];

type HomeAreaCard = {
	code: string;
	title: string;
	description: string;
	href: string;
};

const [areaEntries, authorEntries, blogEntries] = await Promise.all([
	getAreaEntries(),
	getAuthorEntries(),
	getBlogEntries(),
]);
const posts = sortPostsByDate(normalizePosts(blogEntries, authorEntries)).filter(isPublicPost);
const areaDirectory = buildCanonicalAreaDirectory(areaEntries, posts);
const areaByCode = new Map(areaDirectory.map((area) => [area.code, area]));
const areaCards = [...CANONICAL_CATEGORY_DEFINITIONS]
	.sort((a, b) => a.homeOrder - b.homeOrder)
	.map((definition): HomeAreaCard | undefined => {
		const area = areaByCode.get(definition.code);
		if (!area) return undefined;

		return {
			code: definition.code,
			title: definition.homeTitle,
			description: definition.homeDescription,
			href: area.href,
		};
	})
	.filter((card): card is HomeAreaCard => Boolean(card));

const faqSchema = buildFaqSchema(
	faq.map((item) => ({
		question: item.q,
		answer: item.a,
	})),
);

const localBusinessSchema = {
	...LEGAL_SERVICE_BASE_SCHEMA,
	description,
	image: toAbsoluteUrl('/brand/pavie-logo-horizontal.png'),
};
---

<BaseLayout title={title} description={description} canonical={`${MAIN_SITE_URL}/`}>
	<div class="site-home">
		<section class="site-hero" aria-labelledby="hero-title">
			<div class="container site-hero__inner">
				<span class="site-eyebrow">Advocacia estratÃ©gica e acolhedora</span>
				<h1 id="hero-title">Clareza, tÃ©cnica e respeito na conduÃ§Ã£o dos seus direitos.</h1>
				<p class="site-hero__lead">
					Uma advocacia focada na sua seguranÃ§a. Atuamos desde o planejamento e organizaÃ§Ã£o do seu
					patrimÃ´nio atÃ© questÃµes sensÃ­veis de famÃ­lia, curatelas e recuperaÃ§Ã£o de crÃ©ditos.
				</p>
				<div class="site-hero__actions">
					<a class="btn btn-primary" href="#contato">Solicitar anÃ¡lise do caso</a>
					<a class="btn btn-secondary" href="/#areas">Ver Ã¡reas de atuaÃ§Ã£o</a>
				</div>
				<p class="site-hero__location">Sede no Rio de Janeiro <span aria-hidden="true">â€¢</span> Atendimento em todo o Brasil</p>
			</div>
		</section>

		<section id="advogado" class="site-section professional-section container" aria-labelledby="advogado-title">
			<div class="professional-media">
				<img
					src="/uploads/fabio-pavie-profile.png"
					alt="Foto profissional do advogado Fabio Mathias Pavie"
					loading="lazy"
				/>
				<span>Advogado titular</span>
			</div>

			<div class="professional-copy">
				<span class="site-eyebrow">ConduÃ§Ã£o tÃ©cnica e humana</span>
				<h2 id="advogado-title">Fabio Mathias Pavie</h2>
				<blockquote>
					â€œAcredito que a seguranÃ§a jurÃ­dica se constrÃ³i com tÃ©cnica, franqueza e profundo respeito Ã 
					histÃ³ria de cada cliente. O nosso compromisso Ã© organizar os detalhes do seu caso para que
					vocÃª possa tomar decisÃµes claras e proteger o que construiu.â€
				</blockquote>
				<p>
					Inscrito na OAB/RJ 134.947, dedica-se a soluÃ§Ãµes jurÃ­dicas que envolvem estruturaÃ§Ã£o
					patrimonial, direito de famÃ­lia, curatelas e recuperaÃ§Ã£o de crÃ©dito. Prioriza uma advocacia
					prÃ³xima, transparente e de alta qualidade tÃ©cnica.
				</p>
				<a class="text-link" href="/sobre/">Conhecer a trajetÃ³ria do advogado</a>
			</div>
		</section>

		<section id="metodo" class="site-section site-method" aria-labelledby="metodo-title">
			<div class="container section-title section-title--center">
				<span class="site-eyebrow">MÃ©todo</span>
				<h2 id="metodo-title">Como conduzimos o seu caso</h2>
				<p>
					A tranquilidade das suas decisÃµes depende de mÃ©todo e clareza. Conduzimos o seu processo com
					total transparÃªncia e rigor tÃ©cnico, oferecendo a previsibilidade necessÃ¡ria desde o primeiro contato.
				</p>
			</div>

			<ol class="container process-list">
				{processSteps.map((step, index) => (
					<li class="process-step">
						<span class="process-step__icon" aria-hidden="true"></span>
						<h3>{index + 1}. {step.title}</h3>
						<p>{step.text}</p>
					</li>
				))}
			</ol>
		</section>

		<section id="areas" class="site-section site-areas" aria-labelledby="areas-title">
			<div class="container section-title section-title--areas">
				<div>
					<h2 id="areas-title">Ãreas de AtuaÃ§Ã£o</h2>
					<p>
						A complexidade exige foco. A nossa atuaÃ§Ã£o concentra-se em frentes que demandam
						organizaÃ§Ã£o prÃ©via, proteÃ§Ã£o de ativos, cuidado familiar e reparaÃ§Ã£o tÃ©cnica de direitos.
					</p>
				</div>
				<span>Escopo institucional PAVIE</span>
			</div>

			<div class="container area-grid">
				{areaCards.map((area) => (
					<article class="area-card">
						<span class="area-card__icon" aria-hidden="true"></span>
						<h3>{area.title}</h3>
						<p>{area.description}</p>
						<a href={area.href}>Conhecer Ã¡rea <span aria-hidden="true">â†’</span></a>
					</article>
				))}
			</div>
		</section>

		<section id="conteudos" class="editorial-bridge" aria-labelledby="conteudos-title">
			<div class="container editorial-bridge__inner">
				<span class="site-eyebrow">ConteÃºdo educativo</span>
				<h2 id="conteudos-title">Aprofunde a sua compreensÃ£o antes de tomar uma decisÃ£o.</h2>
				<p>
					Se o seu momento ainda Ã© de busca por informaÃ§Ãµes, visite os nossos artigos e guias tÃ©cnicos.
					Eles foram elaborados para traÃ§ar um caminho claro e seguro sobre os seus direitos, preparando
					o terreno de forma informativa para quando decidir falar com o advogado.
				</p>
				<a class="btn btn-secondary" href="/blog/">Acessar artigos e guias</a>
			</div>
		</section>

		<section id="faq" class="site-section faq-section container" aria-labelledby="faq-title">
			<div class="section-title section-title--center">
				<span class="site-eyebrow">Primeiro contato</span>
				<h2 id="faq-title">Respostas iniciais para o primeiro contato</h2>
			</div>

			<div class="faq-list">
				{faq.map((item) => (
					<details class="faq-item">
						<summary>{item.q}</summary>
						<p>{item.a}</p>
					</details>
				))}
			</div>
		</section>

		<section id="contato" class="site-contact" aria-labelledby="contato-title">
			<div class="container site-contact__grid">
				<div class="site-contact__copy">
					<BrandLogo variant="horizontal" theme="dark" width={300} />
					<p>
						SeguranÃ§a jurÃ­dica, direÃ§Ã£o tÃ©cnica apurada pelo respeito, colaboraÃ§Ã£o e excelÃªncia na
						defesa dos seus direitos.
					</p>

					<article class="site-contact__honorarios">
						<h3>TransparÃªncia em honorÃ¡rios</h3>
						<p>
							Adotamos critÃ©rios coerentes com clareza, previsibilidade e sigilo profissional.
							Os honorÃ¡rios sÃ£o apresentados antes do inÃ­cio de qualquer atuaÃ§Ã£o, sem promessas ou
							atalhos incompatÃ­veis com a Ã©tica jurÃ­dica.
						</p>
					</article>

					<ul class="site-contact__links" aria-label="Canais de contato">
						<li><a href="mailto:contato@pavieadvocacia.com.br">contato@pavieadvocacia.com.br</a></li>
						<li><a href="tel:+5521964382263">(21) 96438-2263</a></li>
					</ul>
				</div>

				<form
					id="siteContactForm"
					class="site-contact__form"
					action="/api/contato"
					method="post"
					aria-describedby="site-contact-note"
				>
					<h2 id="contato-title">Solicitar AnÃ¡lise do Caso</h2>
					<p>
						Compartilhe o contexto do seu problema para darmos o primeiro passo. Nossa equipe farÃ¡
						uma leitura atenta da sua situaÃ§Ã£o, oferecendo um retorno cuidadoso e objetivo sobre a
						viabilidade tÃ©cnica, sempre sob rigoroso sigilo profissional.
					</p>

					<input id="turnstileToken" name="turnstileToken" type="hidden" />

					<label>
						<span>Nome completo</span>
						<input id="nome" name="nome" autocomplete="name" required type="text" />
					</label>

					<label>
						<span>Telefone, WhatsApp ou E-mail</span>
						<input id="contato_preferencial" name="contato_preferencial" autocomplete="email" required type="text" />
					</label>

					<label>
						<span>Resumo da situaÃ§Ã£o</span>
						<textarea id="mensagem" name="mensagem" required rows="5"></textarea>
					</label>

					<label class="site-contact__consent">
						<input name="consent" required type="checkbox" />
						<span>Autorizo o uso dos dados enviados apenas para retorno do contato, sob sigilo profissional.</span>
					</label>

					<div
						class="cf-turnstile"
						data-sitekey="0x4AAAAAAB6FBS0cTG_7KOYv"
						data-callback="onTsDone"
						data-expired-callback="onTsExpired"
						data-error-callback="onTsError"
					>
					</div>

					<p id="site-contact-status" class="site-contact__status" role="status" aria-live="polite"></p>

					<button class="btn btn-primary" type="submit">Enviar para o Advogado</button>
					<p id="site-contact-note" class="site-contact__note">
						Contato inicial nÃ£o Ã© contrataÃ§Ã£o automÃ¡tica. A orientaÃ§Ã£o jurÃ­dica depende da anÃ¡lise da situaÃ§Ã£o individual.
					</p>
				</form>
			</div>
		</section>
	</div>

	<script is:inline>
		(function () {
			var tokenField = document.getElementById('turnstileToken');
			var form = document.getElementById('siteContactForm');
			var status = document.getElementById('site-contact-status');

			window.onTsDone = function (token) {
				if (tokenField) tokenField.value = token || '';
				if (status) status.textContent = 'ValidaÃ§Ã£o de seguranÃ§a concluÃ­da.';
			};
			window.onTsExpired = function () {
				if (tokenField) tokenField.value = '';
				if (status) status.textContent = 'ValidaÃ§Ã£o expirada. Aguarde recarregar e tente novamente.';
			};
			window.onTsError = function () {
				if (tokenField) tokenField.value = '';
				if (status) status.textContent = 'NÃ£o foi possÃ­vel carregar a validaÃ§Ã£o de seguranÃ§a.';
			};

			if (!form || !status) return;

			form.addEventListener('submit', async function (event) {
				event.preventDefault();
				var button = form.querySelector('button[type="submit"]');
				var originalText = button ? button.textContent : '';

				if (tokenField && !tokenField.value) {
					status.textContent = 'Aguarde a validaÃ§Ã£o de seguranÃ§a antes de enviar.';
					return;
				}

				if (button) {
					button.disabled = true;
					button.textContent = 'Enviando...';
				}
				status.textContent = 'Enviando informaÃ§Ãµes iniciais...';

				try {
					var response = await fetch(form.action, {
						method: 'POST',
						body: new FormData(form),
						headers: { Accept: 'application/json' },
					});
					var data = await response.json().catch(function () { return {}; });
					if (!response.ok || data.ok === false) {
						throw new Error(data.error || 'Falha no envio.');
					}
					status.textContent = 'SolicitaÃ§Ã£o enviada. A PAVIE farÃ¡ a triagem inicial do contato.';
					form.reset();
					if (tokenField) tokenField.value = '';
					if (window.turnstile && typeof window.turnstile.reset === 'function') {
						window.turnstile.reset();
					}
				} catch (error) {
					status.textContent = error && error.message
						? 'NÃ£o foi possÃ­vel enviar: ' + error.message
						: 'NÃ£o foi possÃ­vel enviar a solicitaÃ§Ã£o.';
				} finally {
					if (button) {
						button.disabled = false;
						button.textContent = originalText;
					}
				}
			});
		})();
	</script>
	<script is:inline src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
	<script is:inline type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
	<script is:inline type="application/ld+json" set:html={JSON.stringify(localBusinessSchema)} />
</BaseLayout>

---
import type { TocItem } from '../../lib/toc';
import ReadingTOC from './ReadingTOC.astro';

interface Props {
	toc: TocItem[];
}

const { toc } = Astro.props;
---

{toc.length > 1 && (
	<aside class="reading-sidebar" aria-label="Sumário do artigo">
		<ReadingTOC items={toc} />
	</aside>
)}

---
const { items = [] } = Astro.props; // [{href,label}] último sem href
---
<nav aria-label="breadcrumb" class="meta">
  {items.map((it, i) => (
    <>
      {i>0 && ' · '}
      {it.href ? <a href={it.href}>{it.label}</a> : <span aria-current="page">{it.label}</span>}
    </>
  ))}
</nav>

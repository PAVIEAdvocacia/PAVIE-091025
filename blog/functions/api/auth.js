// functions/api/auth.js
// Placeholder para GitHub OAuth no Cloudflare Pages.
// Implemente a troca de código/estado com o GitHub e redirecione de volta ao /admin/.
// Consulte a doc oficial do Decap (GitHub backend) e do Cloudflare Pages Functions.
export async function onRequest(context) {
  return new Response("Configure o OAuth do GitHub aqui (auth).", { status: 200 });
}
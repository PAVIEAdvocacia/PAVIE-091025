{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.1.0",
    "pagefind": "^1.0.0"
  }
}
